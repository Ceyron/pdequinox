from setuptools import setup

setup(
    name="pdequinox",
    version="0.0.1",
    description="JAX/Equinox-based architectures for neural operators (=predictors or correctors)",
    author="Felix Koehler",
)
